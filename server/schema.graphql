type Query {
    job(id: ID!): Job
    jobs: [Job!]
    company(id: ID!): Company
}
type Company{
    id: ID!
    name: String!
    description: String
}
type Job {
  id: ID!
  """The date the job was posted"""
  date: String!
  title: String!
  company: Company!
  description: String
}